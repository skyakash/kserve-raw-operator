---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: kserverawmodes.operator.akashdeo.com
spec:
  group: operator.akashdeo.com
  names:
    kind: KServeRawMode
    listKind: KServeRawModeList
    plural: kserverawmodes
    singular: kserverawmode
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KServeRawMode is the Schema for the kserverawmodes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KServeRawModeSpec defines the desired state of KServeRawMode
            properties:
              foo:
                description: Foo is an example field of KServeRawMode. Edit kserverawmode_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: KServeRawModeStatus defines the observed state of KServeRawMode
            properties:
              phase:
                description: Phase indicates the current stage of installation
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
